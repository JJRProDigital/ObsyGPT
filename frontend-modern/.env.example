# En desarrollo (vite dev) dejadlo vacio o comentado: las llamadas /api pasan por el
# proxy de vite.config.ts y funcionan igual desde localhost, 127.0.0.1 o IP de red.
# Solo hace falta en builds de produccion servidas sin proxy, ej.:
# VITE_BACKEND_URL=http://127.0.0.1:8000
VITE_BACKEND_URL=
